# Server-only. Never prefix with NEXT_PUBLIC_.
# Add the same keys in Vercel → Settings → Environment Variables, then redeploy.
RESEND_API_KEY=
CONTACT_TO_EMAIL=jgelsomino@foundry360.us
# Until foundry360.us is verified in Resend, use: Enigma <onboarding@resend.dev>
CONTACT_FROM_EMAIL=Enigma <onboarding@resend.dev>
# Public measurement ID from Google Analytics (starts with G-). Also set in Vercel.
NEXT_PUBLIC_GA_MEASUREMENT_ID=
